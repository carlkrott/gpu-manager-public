title = "GPUManager source-only gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Exact public identifiers that trigger generic-api-key heuristics"
regexTarget = "secret"
regexes = [
    '''^polling_wait=self\._polling_wait$''',
    '''^wan-t2v-esrgan-rife$''',
]
